[package]
name = "tensorrt"
version = "0.1.0"
edition = "2021"

[features]
tracing = ["dep:tracing"]

[dependencies]
cpp-interop = { version = "0.1.0", path = "../cpp-interop" }
tensorrt-sys = { path = "../tensorrt-sys" }
tracing = { workspace = true, optional = true }
